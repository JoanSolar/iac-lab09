name: Saludo

on:
  push:
    branches:
      - main
      - develop

env:
  MI_NOMBRE: "Joan Solar"

jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo desde main
        run: echo "Hola desde main, mi nombre es $MI_NOMBREr"

      - name: Saludo desde develop
        run: echo "Hola desde develop, mi nombre es $MI_NOMBRE"
